package com.example.taskkotlin.utils

    //        val navView: BottomNavigationView = binding.navView
//    setupActionBarWithNavController(navController, appBarConfiguration)
//    navController.addOnDestinationChangedListener { _, destination, _ ->
//        binding.navView.isVisible = navigator.contains(destination.id)
//        if (destination.id  == R.id.onBordFragment||destination.id  == R.id.addTaskFragment)
//        { supportActionBar?.hide() } else supportActionBar?.show()
////            navView.isVisible = destination.id != R.id.addTaskFragment
////            navView.isVisible = destination.id != R.id.onBordFragment

//        setFragmentResultListener(TV_NAME) { requestKey, bundle ->
//            val result = bundle.getSerializable(TV_NUMBER) as Home
//            adapter.addTask(result)
//        }

